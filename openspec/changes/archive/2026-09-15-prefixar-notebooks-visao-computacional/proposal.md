## Why

O integrante pediu para prefixar os 6 notebooks com `visao_computacional_` (identifica a disciplina no nome do arquivo, útil ao submeter/organizar entregas fora do repositório).

## What Changes

- Renomear os 6 notebooks: `01_data.ipynb` → `visao_computacional_01_data.ipynb` (e assim por diante para `02_detection`, `03_segmentation`, `04_evaluation`, `05_inference_video`, `06_report`).
- Atualizar o metadado interno `colab.name` de cada notebook.
- Atualizar as referências de nome em `README.md`.

## Capabilities

### Modified Capabilities
- `repository-packaging`: o requisito de notebooks passa a citar os nomes de arquivo com o prefixo `visao_computacional_`.

## Impact

- Apenas renomeação de arquivos já existentes e atualização de referências — nenhuma mudança de conteúdo/lógica dos notebooks.
